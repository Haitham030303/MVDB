@*@model List<Customer>*@

@{
    ViewData["Title"] = "Actors and Directors";
}

<h1>People</h1>

@*
    @if (@Model.Count == 0)
{
    <text>We currently don't have any customers</text>
}
else
    {

    <table class="table table-bordered table-hover">
        <tr>
            <th>Customer</th>
        </tr>
    @foreach (var customer in @Model)
    {
        <tr>
            <td>@Html.ActionLink(@customer.Name, "Details", "People",
                    new { id = @customer.Id }, null)
            </td>
        </tr>
    }
    </table>
}
*@